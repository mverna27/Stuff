-> dropbox
sudo pacman -S dropbox --needed
then enable autostart

-> /etc/pacman.conf
remove and add to ignore list:
sudo pacman -Rns amdvlk lib32-amdvlk xf86-video-ati xf86-video-dummy xf86-video-fbdev xf86-video-nouveau xf86-video-openchrome xf86-video-qxl xf86-video-vesa xf86-video-vmware xf86-video-voodoo

-> set vim as default editor
/etc/environment
EDITOR=vim

-> firefox: fix performance
about:config
gfx.webrender.all=true
layot.frame_rate=144 and 100 for different profiles

-> firefox: create 2nd desktop application for different profile (refresh rate for example)
https://www.techrepublic.com/article/how-to-run-two-different-firefox-profiles-at-once-on-linux/

-> enable chaotic aur
https://aur.chaotic.cx/

-> xorg: configure displays with xrandr
xrandr --output DisplayPort-0 --set TearFree on --primary --mode 2560x1440 --rate 144 --output DisplayPort-1 --set TearFree on --mode 3440x1440 --rate 100 --left-of DisplayPort-0

-> install packages from pkglist
sudo pacman -S --needed - < pkglist-pacman
yay -S --needed - < pkglist-aur

-> keyboard tweaks
set keyboard delay to 300ms
set keyboard repeat to 50/sec
bind caps lock to escape

-> xorg: disable mouse acceleration
https://wiki.archlinux.org/title/Mouse_acceleration
/etc/X11/xorg.conf.d/50-mouse-acceleration.conf
Section "InputClass"
	Identifier "My Mouse"
	Driver "libinput"
	MatchIsPointer "yes"
	Option "AccelProfile" "flat"
	Option "AccelSpeed" "0"
EndSection

-> xorg: enable VRR, driver vsync, disbale pageflipping to reduce input lag (BREAKS VRR!!!)
/etc/X11/xorg.conf.d/20-amdgpu.conf
Section "Device"
   Identifier "AMD"
   Driver "amdgpu"
   Option "EnablePageFlip" "off"
   Option "TearFree" "true"
   Option "VariableRefresh" "true"
EndSection

-> xorg: reduce DRI latency (THIS BROKE MY ALACRITTY!!!)
add to /etc/drirc or ~/.drirc
<driconf>
   <device>
       <application name="Default">
           <option name="vblank_mode" value="0" />
       </application>
   </device>
</driconf>

-> bash/zsh: tweaks
no sudo password prompt: add to visudo: 
marco ALL=(ALL) NOPASSWD:ALL

-> remove vi if vim is installed
sudo pacman -Rns vi

-> bash: tweaks
~/.bashrc
alias vi=vim
alias gvim=vim
alias view=rview
systemwide case-insensitive bash completion:
echo 'set completion-ignore-case On' | sudo tee -a /etc/inputrc

-> zsh: tweaks
~/.zshrc
alias vi=vim
alias gvim=vim
alias view=rview
if ctrl/alt + left/right arrow key puts me in vi mode: set -o emacs to force emacs mode in terminal
bindkey -- ';5D'   backward-word
bindkey -- ';3D'   backward-word
bindkey -- ';5C'   forward-word
bindkey -- ';3C'   forward-word
bindkey '^H' backward-kill-word
bindkey '5~' kill-word
bindkey '3~' kill-word
sudo pacman -S zsh-syntax-highlighting --needed
then put in zshrc (last line): source /usr/share/zsh/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
bindkey -s '^o' '. ranger\n'

-> vim: tweaks
/etc/vimrc
set nu rnu
filetype plugin on
syntax on
" Replace all is aliased to S
nnoremap S :%s//g<Left><Left>
set clipboard=unnamedplus

-> GRUB: skip timeout
https://wiki.archlinux.org/title/GRUB/Tips_and_tricks#Hide_GRUB_unless_the_Shift_key_is_held_down

-> replace pulseaudio with pipewire + tweaks
sudo pacman -S pipewire pipewire-alsa pipewire-pulse lib32-pipewire easyeffects --needed
download EQ presets from https://forum.manjaro.org/t/howto-enhance-your-linux-audio-with-easyeffects-or-pulseeffects-legacy/82497
enable noise reduction and echo canceller in easyeffects
pactl info

-> alacritty: opacity
~/.config/alacritty/alacritty.yml
background_opacity: 0.8  # value range is 0 ~ 1

-> alacritty: themes
yay -S alacritty-themes dracula-alacritty-git --needed
enable nord theme

-> alacritty: dropdown
yay -S tdrop-git
tdrop -m -a -w "100%" -h "75%" alacritty --class "tdrop"
then exclude class "tdrop" from bismuth tiling
maximize horizontally
ignore requested geometry
add custom shortcut

-> fstab: mount game drive
https://www.youtube.com/watch?v=vAgWug72-mQ
mkdir /mnt/games
sudo chown marco:marco /mnt/games/
add to fstab with following options:
nosuid,nodev,nofail,x-gvfs-show,x-gvfs-name=games,rw,users,exec,auto

-> append $PATH in /etc/profile

-> ranger: copy configs
ranger --copy-config=all

-> ranger: show image previews
cp /usr/share/doc/ranger/config/rc.conf ./confing/ranger/
set preview_images true
set preview_images_method ueberzug

-> ranger: show video previews
sudo pacman -S ffmpegthumbnailer --needed
cp /usr/share/doc/ranger/config/scope.sh .config/ranger
in ~/.config/ranger/scope.sh
uncomment this part:
# Video
 video/*)
     # Thumbnail
     ffmpegthumbnailer -i "${FILE_PATH}" -o "${IMAGE_CACHE_PATH}" -s 0 && exit 6
     exit 1;;

-> ranger bind to Meta+F
/usr/share/applications/ranger.desktop
Terminal=false
Exec= alacritty -e ranger
then set this desktop application for shortcut

-> spotify
google how to use spotifyd with spotify-tui
use custom play-pause, stop, next, previous scripts if spotifyd mediakeys are fucking around

-> configure mpv
https://wiki.archlinux.org/title/Mpv

-> manage dotfiles: bare git repository
atlassian.com/git/tutorials/dotfiles

-> git token
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

#####################################
########## USEFUL COMMANDS ##########
#####################################
xrandr --output DisplayPort-0 --set TearFree on --primary --mode 2560x1440 --rate 144 --output DisplayPort-1 --set TearFree on --mode 3440x1440 --rate 100 --left-of DisplayPort-0
qdbus org.kde.KWin /Compositor suspend
qdbus org.kde.KWin /Compositor resume
WINE_FULLSCREEN_FSR=1 %command%
sed -i 's/\r//g' FileWithCarriageReturns.sh
