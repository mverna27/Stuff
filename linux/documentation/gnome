-> install dropbox
sudo pacman -S nautilus-dropbox --needed

-> install other software
sudo pacman -S dconf-editor hydrapaper chrome-gnome-shell --needed

-> make xorg default displayserver
/etc/gdm/custom.conf
WaylandEnable=false
add to [daemon] section:
DefaultSession=gnome-xorg.desktop

-> if gdm launches on wrong monitor
sudo cp -v ~/.config/monitors.xml /var/lib/gdm/.config/
sudo chown gdm:gdm /var/lib/gdm/.config/monitors.xml

-> change volume step
open dconf-editor and change org.gnome.settings-daemon.plugins.media-keys.volume-step
